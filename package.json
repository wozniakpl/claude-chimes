{
  "name": "claude-chimes",
  "version": "0.0.2",
  "private": true,
  "scripts": {
    "lint": "biome check plugins",
    "prepare": "git config core.hooksPath .githooks"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.0.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "@biomejs/biome"
    ]
  }
}
